export const elementAlreadyExists = name => {
  return `An element with name ${name} already exists.`
}

export const missingInputFields = () => {
  return 'One or more input fields are missing values, please fill them out.'
}
